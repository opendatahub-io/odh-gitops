apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - namespace.yaml
  - operatorgroup.yaml
  - subscription.yaml
  # RHCL/AuthPolicy reconciliation requires a Gateway API provider and a GatewayClass.
  # Without this, the AuthPolicy reports a MissingDependency condition.
  - openshift-default-gwclass.yaml
