{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: rhods-operator-webhook-service
  namespace: {{ .Values.rhaiOperator.namespace }}
spec:
  ports:
    - name: https
      port: 443
      protocol: TCP
      targetPort: 9443
  selector:
    name: rhods-operator
{{- end }}
